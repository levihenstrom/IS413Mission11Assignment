/** Saved list UI state for Continue shopping / restore after add-to-cart. */
export interface ListSnapshot {
  pageNum: number;
  pageSize: number;
  sortTitle: boolean;
  selectedCategories: string[];
}
